const setup = require('../setup');

let resolver;

describe('GraphQL', () => {
  beforeAll(async () => {
    // Setup
    ({ resolver } = await setup());
  });

  test('exec (create)', async () => {

  });
});
